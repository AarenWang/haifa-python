# Runtime dependencies
pytest>=7.0.0

# Development dependencies
# PyInstaller is only needed for building standalone executables
pyinstaller>=5.0.0

# Optional GUI dependencies
pygame>=2.5.0

# Note: Use 'pip install ".[gui]"' to install GUI dependencies
# Use 'pip install ".[dev]"' to install all development dependencies
# Use 'pip install ".[build]"' for CI/CD build tools only

