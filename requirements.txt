# Runtime dependencies
pytest>=7.0.0

# Development dependencies
# PyInstaller is only needed for building standalone executables
pyinstaller>=5.0.0

# Optional: GUI visualizer (uncomment to enable pygame-based GUI)
# pygame>=2.5.0

# Note: Consider using 'pip install ".[dev]" or ".[build]"' instead
# for proper dependency management via pyproject.toml

